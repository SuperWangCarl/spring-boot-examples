<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.carlme.mybatis.mapper.NoticeMapper">

    <select id="getById" resultType="com.carlme.mybatis.bean.Notice">
        SELECT * FROM t_common_notice WHERE id=#{i}
    </select>

</mapper>